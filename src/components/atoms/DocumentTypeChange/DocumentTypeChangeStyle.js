import styled from "styled-components";

export const DocumentTypeChangeStyle = styled.div`
color: var(--mainTextColor);
display: flex;
justify-content: space-between;
align-items: center;
width: 100%;

.title {
    font-size: var(--mainFontSize);
    margin: 0;
}


`